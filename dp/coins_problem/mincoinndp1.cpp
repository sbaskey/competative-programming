#include <bits/stdc++.h>
using namespace std;

int mincoin(int coins[],int m,int v)
{
    int table[v+1];
    table[0]=0;
    for(int i=0; i<v+1; i++)
    table[i]=INT_MAX;

    for(int i=0; i<m; i++)
    {
        if(coins[i]<=v)
        {
            if()
        }
    }


}